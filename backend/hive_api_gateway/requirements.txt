# hive_api_gateway — plain Python service, no ROS 2 / colcon dependency.
fastapi>=0.115
uvicorn[standard]>=0.30
aiomqtt>=2.0
Pillow>=10.0
PyYAML>=6.0
httpx>=0.27   # proxies WebRTC SDP offers to hive_camera_bridge (/webrtc/offer)
